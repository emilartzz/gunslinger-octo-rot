class Options extends rune.scene.Scene{

  constructor() {
    super();
  }

  init() {
    super.init();
  }

  update(step) {
    super.update(step);
  }

  dispose() {
    super.dispose();
  }

}

Gunslinger.scene.Options = Options;